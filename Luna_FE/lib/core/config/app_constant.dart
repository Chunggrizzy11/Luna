abstract final class AppConstant {
  static const appName = 'Luna';
  static const defaultLocale = 'vi';
  static const networkTimeout = Duration(seconds: 15);
  static const defaultCacheTtl = Duration(minutes: 15);
}
